<%= form_with(model: [ writing, correction ]) do |f| %>
    <div class="form-control">
        <%# Correction Input %>
        <%= f.rich_text_area :body %>
        <%# Submit Button %>
        <%= f.submit submit_label, class: "btn btn-primary mt-1" %>
    </div>
<% end %>