<%= form_with(model: [ writing, writing.corrections.build ]) do |f| %>
    <div class="form-control">
        <%# Correction Input %>
        <%= f.rich_text_area :body %>
        <%# Submit Button %>
        <%= f.submit "Reply", class: "btn btn-primary mt-1" %>
    </div>
<% end %>